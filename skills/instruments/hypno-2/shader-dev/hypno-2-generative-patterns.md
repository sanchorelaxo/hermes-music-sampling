{'Example': 'star-bright.frag\n\n```glsl\nprecision mediump float;\n\nuniform vec2 resolution;\nuniform float time;\n\nuniform float speed;        // CC 0 — animation speed (0.0 = static)\nuniform float orbit_radius; // CC 1 — orbit radius\nuniform float spike_count;  // CC 2 — number of points\nuniform float brightness;   // CC 3 — overall brightness\nuniform float color_shift;  // CC 4 — hue offset\n\nvoid main() {\n    vec2 uv = gl_FragCoord.xy / resolution;\n    vec2 p = (gl_FragCoord.xy - 0.5 * resolution) / resolution.y;\n    float t = time * speed;\n\n    float a = atan(p.y'}